name: curriculum-v
namespace: curriculum-v

imageTag: "1.0.0"

replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

schedulingExclusion:
  excludedNodeLabel: "node-role.kubernetes.io/control-plane"

httpRoute:
  hostname: "cv.theit.cc"
  parentRef: "nginx-theit-cc-1"
